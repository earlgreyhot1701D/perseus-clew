{
  "name": "perseus-clew-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Perseus Clew scan engine. Lambda handlers, check modules, scoring.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "build": "cp -r src dist",
    "dev": "node scripts/local-server.js",
    "init:local": "node scripts/init-local-db.js",
    "benchmark": "node scripts/run-benchmark.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^9.0.0",
    "globals": "^14.0.0",
    "vitest": "^3.1.0"
  },
  "dependencies": {
    "@apidevtools/swagger-parser": "^12.1.0",
    "@aws-sdk/client-bedrock-runtime": "^3.1063.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "cheerio": "^1.0.0",
    "yaml": "^2.9.0"
  },
  "license": "Apache-2.0"
}
